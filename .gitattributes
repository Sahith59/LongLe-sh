# Wrangler generates this runtime declaration file. Preserve it byte-for-byte and
# do not treat generator-owned trailing spaces as repository whitespace errors.
packages/relay/worker-configuration.d.ts -whitespace
